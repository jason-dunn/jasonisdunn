@page "/"
@page "/session/"
@page "/index/{ppguidCurrentGuid:guid?}"
@layout MainLayout
@using jasonisdunn.Shared.Layouts;
@using jasonisdunn.Shared.States;
@using jasonisdunn.Data;
@using System;
@using System.Linq;

@inject MainState AppState
@inject PageHistoryState PageHistoryState
@inject Blazored.LocalStorage.ILocalStorageService localStore
@inject Blazored.SessionStorage.ISessionStorageService localSession

@code{

    [Parameter]
    public Guid? ppguidCurrentGuid { get; set; }
    private Guid CurrentGuid;

    protected override void OnInitialized()
    {
        AppState.OnChange += StateHasChanged;
        PageHistoryState.AddPageToHistory(""); 
    }

    //protected override async Task OnAfterRenderAsync(bool firstRender)
    //{
    //    if (firstRender)
    //    {

    //    }
    //    else
    //    {

    //    }
    //}

    //private async Task SetIndex()
    //{
    //    await localSession.SetItemAsync("index", true); 
    //}
}




